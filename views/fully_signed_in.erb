<!DOCTYPE html>
<html lang="en" class="route-documentation">
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Spotify Slack Status</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bulma/0.4.0/css/bulma.min.css" media="screen">
  <link rel="stylesheet" href="/main.css" media="screen">
</head>
<body>
  <section class="hero is-primary">
    <div class="hero-body">
      <div class="container">
        <nav class="nav">
          <div class="nav-left">
            <a class="title" href="/user/<%= @user.to_param %>">
              Spotify Slack Status
            </a>
          </div>
          <div class="nav-right nav-menu">
            <a href="/logout" class="nav-item">
              Log out
            </a>
          </div>
        </nav>
      </div>
    </div>
  </section>
  <section class="section">
    <div class="container content">
      <p class="has-text-centered">
        You are signed in as <strong><%= @user.email %></strong>.
        <% if @team_name.present? %>
          You are authenticated with the
          <% if @team_image.present? %>
            <img class="avatar" src="<%= @team_image %>" alt="<%= @team_name %> icon" width="22" height="22">
          <% end %>
          <strong><%= @team_name %></strong>
          team on Slack.
        <% end %>
      </p>
      <% if @currently_playing.present? %>
        <div class="columns">
          <div class="column is-half is-offset-one-quarter">
            <form method="POST" action="/update-status" class="box">
              <article class="media">
                <div class="media-content">
                  <p>
                    🎵 Listening to: <%= @currently_playing %>
                  </p>
                  <input type="hidden" name="status" value="Listening to: <%= @currently_playing.gsub(/"/, "'") %>">
                  <button type="submit" class="button is-slack">
                    Update Slack Status
                  </button>
                </div>
              </article>
            </form>
          </div>
        </div>
      <% end %>
    </div>
  </section>
</body>
</html>
